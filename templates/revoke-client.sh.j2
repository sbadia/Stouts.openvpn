#!/bin/sh
#
# {{ ansible_managed }}
# Do NOT modify this file by hand!

. {{openvpn_etcdir}}/vars

[ $# -lt 1 ] && echo "Usage: $0 <CLIENT>"

$EASY_RSA/revoke-full $@

export CLIENT=$1

rm -rf $KEY_DIR/$CLIENT*
